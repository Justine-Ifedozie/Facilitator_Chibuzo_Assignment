//initialize a String word in a variable or receive user input for the (String word) or (character ch)
//use for loop to iterate through the length of String word.
//


public class findFirstOccurence {
        public static void main(String[] args) {
                String userInput = "abcdefd";
                char ch = 'd';

                reverseWord(userInput);

        }
        
        public static String reverseWord(String userWord) {
                for (int index = 0; index < userNum.length; index++) {
                        String reversed = userInput % 
                }
        
        }
}