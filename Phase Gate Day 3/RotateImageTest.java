import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class RotateImageTest {

        @Test
        public void testThatRotateImagefunctionExist() {
                RotateImage rotate = new RotateImage();
               assertEquals(self);
        }
}
